import React from 'react';

const ContactAgentButton = ({ phoneNumber }) => {
  const handleContactAgent = () => {
    
  };

  return (
    <button onClick={handleContactAgent}>
      Contact Agent
    </button>
  );
};

export default ContactAgentButton;